{
  "name": "moj-projekt",
  "version": "1.0.0",
  "description": "Nowy package.json",
  "main": "app.js",
  "scripts": {
    "dev": "node app.js",
    "start": "node app.js",
    "build": "next build",
    "test": "echo 'Error: no test specified' && exit 1"
  },
 "dependencies": {
  "socket.io-client": "^4.5.1",
  "express": "^4.17.1",
  "react": "^18.0.0"
}
  "postinstall": "rm -rf node_modules && rm -f package-lock.json && npm install"
}
